{
  "fork": "@subsquid/ponder",
  "upstream": "ponder",
  "policy": "Version = `<ponder-version>-sqd.<rev>`: @subsquid/ponder@X.Y.Z-sqd.N is built from ponder@X.Y.Z + the Portal layer (rev N). The ponder version stays visible; bump `-sqd.<rev>` for a fork-side fix on the SAME ponder version (npm permanently retires an unpublished version number, so a plain mirror like 0.16.6 can't be re-cut after a bad publish). Published with `--tag latest` so the prerelease-form version is the default `npm install`. The bin stays `ponder` (drop-in). We cut a release only for ponder versions a client needs; `compat.tested` records which versions the seam is proven against.",
  "layer": "portal/",
  "seam": "HistoricalSync.syncBlockRangeData / syncBlockData (interval-scoped) — identical in shape across the tested range",
  "versions": [
    {
      "ponder": "0.16.6",
      "wiring": "wiring/0.16.6.patch",
      "rev": 1,
      "status": "verified",
      "published": false,
      "note": "bare 0.16.6 seed-publish (2026-06-30) is BROKEN (workspace: deps) — unpublish it; ship 0.16.6-sqd.1 once the byte-identity + correctness fixes land"
    },
    {
      "ponder": "0.15.17",
      "wiring": "wiring/0.15.17.patch",
      "status": "planned",
      "published": false,
      "note": "Euler (the company) pins this; seam verified identical 0.15.17→0.16.6. Generate the wiring patch + run sync-upstream to promote to verified."
    }
  ],
  "compat": {
    "tested": ["0.16.6"],
    "seamStableRange": "0.15.17 - 0.16.6 (verified identical by inspection; CI matrix promotes to 'tested' as patches land)",
    "howToAdd": "see PUBLISHING.md — scripts/sync-upstream.sh <ver> --test, then add the row here"
  }
}
