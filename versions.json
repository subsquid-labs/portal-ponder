{
  "fork": "@subsquid/ponder",
  "upstream": "ponder",
  "policy": "Version = `<ponder-version>-sqd.<rev>`: @subsquid/ponder@X.Y.Z-sqd.N is built from ponder@X.Y.Z + the Portal layer (rev N). The ponder version stays visible; bump `-sqd.<rev>` for a fork-side fix on the SAME ponder version (npm permanently retires an unpublished version number, so a plain mirror like 0.16.6 can't be re-cut after a bad publish). The NEWEST ponder version is published with `--tag latest` (the default `npm install`); an older version publishes under a `ponder-<version>` dist-tag instead of clobbering `latest`. The bin stays `ponder` (drop-in). We cut a release only for ponder versions a client needs; `compat.tested` records which versions the seam is proven against.",
  "layer": "portal/",
  "nextFromMain": "0.16.6-sqd.2 — the invariant-first re-architecture of portal/ (functional core + imperative shell, INV-1…INV-18, portal/INVARIANTS.md; fixes G1/G2/G3). Behavior + seam frozen; the full Portal property/regression suite (272 tests) passes. DO NOT publish from this PR — publish is a separate, deliberate step.",
  "seam": "HistoricalSync.syncBlockRangeData / syncBlockData (interval-scoped) — identical in shape across the tested range",
  "versions": [
    {
      "ponder": "0.16.7",
      "wiring": "wiring/0.16.7.patch",
      "rev": 1,
      "status": "verified",
      "published": false,
      "note": "Seam identical to 0.16.6 — the 0.16.6 wiring patch applies cleanly (zero rejects, 10/10 files), builds, and passes the full Portal suite (272 tests) on ponder@0.16.7 (CI matrix). 0.16.6→0.16.7 is one DB-layer PR (ponder-sh/ponder#2314: live-query notification batching, drops the 100-table limit) touching database/actions, drizzle/onchain, build/schema, bin/createViews — none of the graft surfaces (sync-historical, runtime/historical, sync-store, rpc, sync). No fresh RPC byte-diff on 0.16.7; compat rests on seam-identity + the full suite, as for 0.15.17."
    },
    {
      "ponder": "0.16.6",
      "wiring": "wiring/0.16.6.patch",
      "rev": 1,
      "status": "verified",
      "published": true,
      "note": "0.16.6-sqd.1 is published (--tag latest): byte-identical vs RPC across logs/tx/receipts/traces (harness/diff) + C1/C4/C6/C7/C8 fixes."
    },
    {
      "ponder": "0.15.17",
      "wiring": "wiring/0.15.17.patch",
      "rev": 1,
      "status": "verified",
      "published": true,
      "note": "Seam identical to 0.16.6 — the 0.16.6 wiring patch applies cleanly, builds, and passes the full portal suite on ponder@0.15.17 (CI matrix)."
    }
  ],
  "compat": {
    "tested": ["0.16.7", "0.16.6", "0.15.17"],
    "seamStableRange": "0.15.17 - 0.16.7 (verified identical by inspection; CI matrix promotes to 'tested' as patches land)",
    "howToAdd": "see PUBLISHING.md — scripts/sync-upstream.sh <ver> --test, then add the row here"
  }
}
