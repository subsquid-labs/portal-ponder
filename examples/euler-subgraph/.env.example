# Historical backfill from SQD Portal (public portal works with no key; a dedicated portal is faster)
PORTAL_URL_1=https://portal.sqd.dev/datasets/ethereum-mainnet
# PORTAL_API_KEY=                       # only for a dedicated/keyed portal
# Realtime + the vault-metadata readContract calls use a normal archive RPC:
PONDER_RPC_URL_1=https://eth.your-archive-rpc.example
# Optional bounded backfill window:
PONDER_START=20529207
PONDER_END=20620000
